
  PUSH_ENUM(FLASH_BEGIN)

  PUSH_ENUM(PRIVATE)
  PUSH_ENUM(LOGICAL)

  PUSH_ENUM(GROUPS)

  PUSH_ENUM(TRANS_ENG)
  PUSH_ENUM(TRANS_CNT)
  PUSH_ENUM(PULSE_HOU)
  PUSH_ENUM(PULSE_MNT)

  PUSH_ENUM(COUNT)
  PUSH_ENUM(LOSSE)
  PUSH_ENUM(LEVEL)

  PUSH_ENUM(IMPHOUCAN)

  PUSH_ENUM(IMPDAYCAN)
  PUSH_ENUM(IMPMONCAN)
  PUSH_ENUM(POWDAYGRP)
  PUSH_ENUM(POWMONGRP)
  PUSH_ENUM(CNTMONCAN)

  PUSH_ENUM(DEFDAYCAN)
  PUSH_ENUM(DEFMONCAN)

  PUSH_ENUM(PORTS)
  PUSH_ENUM(LOCAL_DISABLE)

  PUSH_ENUM(MAJOR_IN_DELAY)
  PUSH_ENUM(MINOR_IN_DELAY)

  PUSH_ENUM(ENBL_CAN)
  PUSH_ENUM(ENBL_PRT_HOU)
  PUSH_ENUM(CTRL_HOU)
  PUSH_ENUM(RECALC_HOU)

  PUSH_ENUM(PHONES)

  PUSH_ENUM(KEYS)

  PUSH_ENUM(ADDRESS1)
  PUSH_ENUM(ADDRESS2)

  PUSH_ENUM(OBJECT_NAME)
  PUSH_ENUM(CANALS_NAME)
  PUSH_ENUM(GROUPS_NAME)

  PUSH_ENUM(PARAMS)
  PUSH_ENUM(PARAMS_DIV)
  PUSH_ENUM(ENBL_PARAMS)

  PUSH_ENUM(PARAMS_VALUES)

  PUSH_ENUM(EXT_4_ENBL_CAN)
  PUSH_ENUM(EXT_4_MON_VALUES)

  PUSH_ENUM(EXT_4T_MON_VALUES)

  PUSH_ENUM(EXT_5_VALUES)

  PUSH_ENUM(EXT_6_BOX_VALUES)
  PUSH_ENUM(EXT_6_MON_VALUES)

  PUSH_ENUM(CORRECT1)

  PUSH_ENUM(KEY_RECORD)
  PUSH_ENUM(SYS_RECORD)
  PUSH_ENUM(DIG_RECORD)
  PUSH_ENUM(IMP_RECORD)
  PUSH_ENUM(MOD_RECORD)
  PUSH_ENUM(PH2_RECORD)
  PUSH_ENUM(AUX_RECORD)

  PUSH_ENUM(FLASH_END)
