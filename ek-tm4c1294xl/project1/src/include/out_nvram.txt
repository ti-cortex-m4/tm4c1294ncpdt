
  PUSH_ENUM(NVRAM_BEGIN)

  PUSH_ENUM(LABEL_GLOBAL)

  PUSH_ENUM(PTR_MNT)
  PUSH_ENUM(PTR_HHR)
  PUSH_ENUM(PTR_DAY)
  PUSH_ENUM(PTR_MON)
  PUSH_ENUM(PTR_TIM)

  PUSH_ENUM(ENBL_KEYS)

  PUSH_ENUM(DIGITALS)

  PUSH_ENUM(RELAXS_FLAG)

  PUSH_ENUM(GAPS_FLAG)

  PUSH_ENUM(SEPARATE_CAN)
  PUSH_ENUM(SHOW_MESSAGES)
  PUSH_ENUM(HIDE_MESSAGES)
  PUSH_ENUM(CONTROL_TIME)
  PUSH_ENUM(MANAGE_TIME)

  PUSH_ENUM(FIRST_RESET)

  PUSH_ENUM(EXT_4_FLAG)
  PUSH_ENUM(EXT_4_MONTHS)
  PUSH_ENUM(EXT_4T_FLAG)
  PUSH_ENUM(EXT_4T_MONTHS)
  PUSH_ENUM(EXT_5_FLAG)
  PUSH_ENUM(EXT_6_FLAG)

  PUSH_ENUM(DSBL_ESC_U)
  PUSH_ENUM(DSBL_ESC_V)
  PUSH_ENUM(DSBL_ESC_S)
  PUSH_ENUM(MNT_ESC_S)
  PUSH_ENUM(EXTENDED_ESC_U)
  PUSH_ENUM(EXTENDED_ESC_V)
  PUSH_ENUM(EXTENDED_ESC_S)

  PUSH_ENUM(ESC_S_VALUE)
  PUSH_ENUM(ESC_S_TIME)
  PUSH_ENUM(ESC_V_VALUE)
  PUSH_ENUM(ESC_V_TIME)
  PUSH_ENUM(ESC_U_VALUE)
  PUSH_ENUM(ESC_U_TIME)

  PUSH_ENUM(EXT_7_DAY_VALUES)

  PUSH_ENUM(MACHINES_ESC)
  PUSH_ENUM(BLOCK_ESC)

  PUSH_ENUM(STRICT_UNI)
  PUSH_ENUM(MAX_DELAY_UNI)
  PUSH_ENUM(REPEAT_FLOW_INI)
  PUSH_ENUM(ENBL_PASS_INI)

  PUSH_ENUM(ENBL_CURRENT)
  PUSH_ENUM(ENBL_PROFILE)
  PUSH_ENUM(TIMEOUT_CURRENT)
  PUSH_ENUM(TIMEOUT_PROFILE)

  PUSH_ENUM(DSBL_REFILL)
  PUSH_ENUM(USE_BOUNDS)
  PUSH_ENUM(MAX_REPEATS)
  PUSH_ENUM(RECALC_ALWAYS)

  PUSH_ENUM(FLOW_PORT_FROM)
  PUSH_ENUM(MAX_FLOW_DELAY)
  PUSH_ENUM(FLOW_PORT_TO)

  PUSH_ENUM(PARAMS_FLAG)
  PUSH_ENUM(MNT_PARAMS)

  PUSH_ENUM(FIX_PARAMS_BUGS)
  PUSH_ENUM(USE_PARAMS_DIV)

  PUSH_ENUM(CORRECT2_FLAG)

  PUSH_ENUM(NVRAM_END)
