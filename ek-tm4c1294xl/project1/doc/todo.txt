3 мин - 24 часа
30 мин - 100 суток

install ccs6
use cg_xml

key_SetDigitals MakeInDelays

VALUE6_CAN_PAGES ? VALUE4_CAN_PAGES

Delphi hex to float

mpdwChannelsB[i] > 0xF0000000) dev_a dev_b 

optimise SetTime & SetDigitals

чтение из RAM ROM DF NVRAM

reset all buffers remotely

P79 и другие

быстрое чтение счетчиков и мгнов парам.
gamma - быстрое задание каналов и параметров

u_hhr u_mnt wSize
x

UART FIFO + sem2reader time

sem2reader расчет макс. мощности по получасам
sem2reader фильтраци€ по названию

output - from mem_profile

убрать dbKtrans

remove external headers

hhr100
params
! вывод дампа обмена со счетчиком
SMK
realtime

записано/прочитано байтов (страниц) в секунду

profiles /4

extended console + server push
console in local loops

*3     00:00:00 00.00.00 140    ../self/tariffs/tariffs.c 

максимуму по 3-мин мощности
макс/мин по мгновенным параметрам

doc информаци€ по программированию каждого счетчика
doc programs2

чтение всех переменных из NVRAM
++iwMajor > GetMaxShutdown()

use CCS6
show time 10 Hz
внутри проекта SW-TM4C-2.1.1.71
RTC temperature ?
systick in ms

FIFO
CRC метка времени + код запрос/ответ

Х	 Up to 256-KB Flash
Х	 Up to 32-KB single-cycle SRAM and 2-KB EEPROM
Х	 Ethernet MAC+PHY ???

delphi конфигураци€ GPIO 

http://infocenter.arm.com/help/index.jsp?topic=/com.arm.doc.dui0349a/Babjdbff.html

GetInBuff(1) -> GetInBuffInt[Big|Low]End(1)
InBuff -> InBuffChar
remove GetInBuff
добавить OutBuff

msg_limit