extended4  report52 счетчики из буфера с дозапросом (на конец мес€ца)
extended4t reportXX счетчики из буфера (по тарифам) с дозапросом (на конец мес€ца)
extended5  report18a счетчики по тарифам на начало текущих суток
extended6  report52 счетчики из буфера с пр€мого опроса (на конец мес€ца)
extended7  reportXX счетчики (на начало суток)

удвоенна€ точность по программам, CRC, ”ѕ»ќ по энергии и показани€м счетчиков
3 мин - 24 часа
30 мин - 100 суток

key_SetDigitals MakeKeys
key_SetDigitals MakeInDelays

заданее телефона с большего на меньший

Esc транзит
Esc console

CRC 32h 50d bINQ_GETPARAM_FULL

ext4, ext4t(+), ext6 - next month
VALUE6_CAN_PAGES ? VALUE4_CAN_PAGES

ext5 push value5

P90  + '-' задание всех каналов одного счетчика

Delphi hex to float

mpdwChannelsB[i] > 0xF0000000) dev_a dev_b 

remove tiKey

optimise SetTime

test int program cnt на вызов (timer + serial) + частота timer

несовместимость старого и нового проборов по ext4

чтение из RAM ROM DF NVRAM