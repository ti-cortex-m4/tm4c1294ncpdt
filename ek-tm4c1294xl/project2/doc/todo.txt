TCP keepalive
DS Manager: locator
проверять возвращаемые значения, особенно MEM
udp_remove & pbuf_free
serial test overflow
serial4 символьные константы ((dwStatus & 0x00000020) != 0) || (dwStatus == 0);
 